import React from "react";
import PlaylistImage from "../components/PlaylistImage";

export default {
  title: "PlaylistImage",
  component: PlaylistImage,
};

const Template = (args) => <PlaylistImage {...args} />;

export const FirstStory = Template.bind({});
